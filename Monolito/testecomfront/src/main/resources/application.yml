spring:
  application:
    name: testecomfront
  datasource:
    url: jdbc:mysql://localhost:3306/link_tree?createDatabaseIfNotExist=true
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect